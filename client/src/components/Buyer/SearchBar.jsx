import { TextField } from "@mui/material";

function SearchBar() {
  return (
    <TextField
      fullWidth
      label="Search fabrics, suppliers..."
      variant="outlined"
    />
  );
}

export default SearchBar;